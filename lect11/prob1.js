// take two inputs from the user and add them
let num1 = Number(prompt("Enter first number:"));
let num2 = parseInt(prompt("Enter second number:"));

console.log(num1 + num2); // this will concatenate the two numbers as strings
